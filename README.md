# user-management-api
This project is a user API built to practice backend development concepts. It implements CRUD operations (GET, POST, PUT, DELETE) and follows a structured approach, including environment setup, models, routes, and controllers. The project also includes unit and integration tests, along with a CI/CD pipeline to ensure code quality and automation.
